diff --git a/app/controllers/dashboard_controller.rb b/app/controllers/dashboard_controller.rb
index d80c364..f7bd673 100644
--- a/app/controllers/dashboard_controller.rb
+++ b/app/controllers/dashboard_controller.rb
@@ -38,6 +38,8 @@ class DashboardController < ApplicationController
   def maybe_render_landing
     return if signed_in?
 
-    render 'pages/landing'
+    portal_url = ENV.fetch('MAIOCCHI_HOME_URL', '/').delete_suffix('/')
+
+    redirect_to "#{portal_url}/#advogados", allow_other_host: true, status: :see_other
   end
 end
